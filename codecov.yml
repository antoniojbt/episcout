#comment: false

codecov:
  # Codecov token should be provided via the CODECOV_TOKEN secret

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto
        threshold: 1%
