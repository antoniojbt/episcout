# Reviews

Use this directory for review notes that apply across multiple specs.

Spec-specific reviews should live in each spec directory as `review.md`.

Keep open cross-spec reviews directly under this directory and move completed reviews to `done/`.
