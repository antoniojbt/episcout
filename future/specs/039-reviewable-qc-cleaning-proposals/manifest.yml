id: "039"
slug: "reviewable-qc-cleaning-proposals"
workflow_version: 1
deliverable: implementation
status: review
owner: repository-owner
base_branch: master
working_branch: agent/issue-271-planning-add-reviewable-qc-and-cleaning-rule
created: 2026-08-12
updated: 2026-08-12

tracking_issue: 271
source_issues:
  - 249
pull_request: 299
merge_commit: null
successor_issue: 272
terminal_reason: null

dependencies:
  suggests:
    - RPostgres

related_files:
  - R/eda_qc_proposals.R
  - R/eda_postgres_queries.R
  - tests/testthat/test-eda-qc-proposals.R
  - README.md
  - vignettes/specification-first-eda.Rmd
  - NEWS.md

documents:
  brief: brief.md
  sdd: sdd.md
  tdd: tdd.md
  acceptance: acceptance.md
  review: review.md

review_required: true

checklists:
  - checklists/software-verification.md
  - checklists/truth-and-semantics.md
  - checklists/analysis-and-statistics.md
  - checklists/copy-edit.md

check_commands:
  - scripts/rscript_env_caller.R -e "options(repos = c(CRAN = 'https://cloud.r-project.org')); devtools::test(filter = 'eda-(qc-proposals|spec-scaffold|summaries|postgres-source)|db-dictionary', reporter = 'summary')"
  - EPISCOUT_TEST_POSTGRES=1 scripts/rscript_env_caller.R -e "options(repos = c(CRAN = 'https://cloud.r-project.org')); devtools::test(filter = 'eda-(qc-proposals|postgres-parity)', reporter = 'summary')"
  - scripts/rscript_env_caller.R -e "devtools::load_all(quiet = TRUE); findings <- lintr::lint_package(); print(findings); if (length(findings) > 0L) quit(status = 1L)"
  - scripts/check-local.sh
  - scripts/check-cran.sh
  - scripts/check-workflow-state.sh
  - git diff --check

activation_gates:
  - accepted: PR-298 is merged at canonical commit-9f48a06 with green checks, and the opaque variable-key, evidence, pending-proposal and approval-boundary contracts are accepted before package changes.
  - accepted: The semantic specification remains unchanged and authoritative; proposal fields are separate and never overwrite type, units, levels, min, max or missing_codes.
  - accepted: The public operation is read-only, returns aggregate tables only and has no file, data-cleaning, rule-approval or PostgreSQL-write path.
  - accepted: Equivalent data-frame and PostgreSQL sources use the same reviewed specification and caller-managed variable-key map, with no categorical value collection.

baseline:
  canonical_master: "9f48a06e457371bfbde3280b54964ea19a5f0af6"
  roadmap: "Issue 249 is authoritative; issue 271 is the active dispatch target, followed by issues 272 and 273."
  semantic_dictionary: "The lean data-frame scaffold has 15 semantic fields; the PostgreSQL dictionary uses source_schema, source_table and source_column as internal source keys and produces the same semantic specification."
  descriptive_summaries: "Canonical summaries already define aggregate counts, observed numeric extrema and Tukey 1.5-IQR fences for data frames and PostgreSQL, but they return source names and complete categorical frequencies."
  preparation: "epi_eda_prepare ignores semantic min and max as cleaning rules and performs no cleaning-rule proposal or approval workflow."

notes:
  - The planning contribution changed no package source, tests, generated help or runtime behaviour.
  - The package implementation begins only after the planning review gate was satisfied by merged PR-298.
  - Issue 272 is the approved-rule application successor after issue 271 implementation and canonical closeout.
  - Issue 273 remains staged after issue 272 and is outside this specification.
