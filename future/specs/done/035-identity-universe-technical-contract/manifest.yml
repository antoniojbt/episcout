id: "035"
slug: "identity-universe-technical-contract"
workflow_version: 1
deliverable: implementation
status: completed
owner: repository-owner
base_branch: master
working_branch: agent/issue-278-refactor-make-the-epi-sec-identity-universe
created: 2026-08-12
updated: 2026-08-12

tracking_issue: 278
source_issues:
  - 274
  - 275
  - 276
pull_request: 281
merge_commit: "ebd8d355a42c697b6747d4e53ef356bde78aa573"
successor_issue: null
terminal_reason: "The required registry successor is intentionally staged only after this closeout is canonical; it remains inactive until its own promotion gate."

dependencies:
  imports:
    - DBI
    - openssl
  suggests:
    - RPostgres

related_files:
  - R/epi_sec_identity_universe.R
  - tests/testthat/test-sec-identity-universe.R
  - tests/testthat/test-sec-identity-universe-postgres.R
  - man/epi_sec_identity_universe_spec.Rd
  - man/epi_sec_identity_universe_db.Rd
  - vignettes/longitudinal-pseudonymisation.Rmd
  - PROJECT_MAP.md
  - future/README.md
  - future/TODOs.md
  - future/changelog.md

documents:
  brief: brief.md
  sdd: sdd.md
  tdd: tdd.md
  acceptance: acceptance.md
  review: review.md

review_required: true

checklists:
  - checklists/software-verification.md
  - checklists/truth-and-semantics.md
  - checklists/analysis-and-statistics.md
  - checklists/copy-edit.md
  - checklists/render-and-release.md

check_commands:
  - scripts/rscript_env_caller.R -e "options(repos = c(CRAN = 'https://cloud.r-project.org')); devtools::test(filter = 'sec-identity-universe', reporter = 'summary')"
  - EPISCOUT_TEST_POSTGRES=1 PGHOST=127.0.0.1 PGPORT=55432 PGDATABASE=synthetic_records PGUSER=postgres scripts/rscript_env_caller.R -e "options(repos = c(CRAN = 'https://cloud.r-project.org')); devtools::test(filter = 'sec-identity-universe-postgres', reporter = 'summary')"
  - scripts/rscript_env_caller.R -e "devtools::load_all(quiet = TRUE); findings <- lintr::lint_package(); print(findings); if (length(findings) > 0L) quit(status = 1L)"
  - scripts/check-local.sh
  - scripts/check-workflow-state.sh
  - git diff --check

activation_gates:
  - accepted: Design PR-277 merged and issue-276 completed post-merge closeout on canonical master before issue-278 was dispatched.
  - accepted: Issue-278 and completed spec-034 define the exact version-2 metadata, result, privilege-neutrality and compatibility contracts.
  - accepted: The change is limited to the identity-universe slice; registry, linkage and pseudonymisation privilege behaviour remains deferred.
  - accepted: The independent two-source fixture fixes the required union, overlap, duplicate and directional-coverage results before implementation.

baseline:
  canonical_master: "afe109bc86245b5b070d906c55e2f93ada913e8c"
  predecessor: "PR-277 merged as commit-8641abe; issue-276 and spec-034 are completed through closeout commit-cc05cb0."
  constructor: "Version 1 requires six source columns and validation_status = confirmed."
  database: "Audit reports blocked for error findings; materialisation refuses PUBLIC schema privileges and revokes PUBLIC table privileges."
  verification: "The initial guard was structurally consistent but lacked an upstream remote; the same canonical URL was added locally, the instructions-only commit-afe109b was integrated and the final online guard passed."

notes:
  - No released 0.3.0 interface contains these two exports; the bounded legacy constructor adapter protects current repository syntax only.
  - Ordinary results remain aggregate and value-free, and native database failures remain sanitised.
  - Focused offline/live PostgreSQL 18.4 tests, package lint, rendered-vignette inspection, the complete local check, workflow state and diff checks pass.
  - The registry successor must be created only after this closeout is canonical and must remain inactive until its separate promotion gate.
