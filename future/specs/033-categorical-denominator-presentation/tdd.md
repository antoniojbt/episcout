# Test Design

Spec ID: `033-categorical-denominator-presentation`
Status: Draft

## Independent Fixture

Use a hand-authored seven-row categorical fixture stratified by `arm`: three `A` rows, three `B` rows, one missing-stratum row and a declared empty `C` group. The displayed `status` variable contains declared `no`, `yes` and `unused` levels, unexpected `other`, one ordinary `NA` and one declared missing code. Expected counts, denominators and proportions are literal test values and are not generated by production helpers.

Expected compatibility examples are `A/yes = 1/2`, `A/missing = 1/3`, `B/other = 1/2`, `Overall/yes = 2/5` and `Overall/missing = 2/7`. Expected column examples are `A/yes = 1/3` and `Overall/yes = 2/7`. Expected row examples are `A/yes = 1/2`, `B/yes = 1/2`, `Overall/yes = 2/2`, missing-stratum/unused `= 1/1`, and every zero-denominator `C` cell has `proportion = NA_real_`. Expected overall examples include `A/yes = 1/7`, `B/other = 1/7` and `Overall/missing = 2/7`.

## Calculation Tests

- [ ] Exact canonical and stratified output schemas, types, ordering, input non-mutation and empty outputs.
- [ ] Literal compatibility, column, row and overall numerator/denominator/proportion values for ordinary, unexpected and missing levels.
- [ ] Overall exclusion from row denominators and inclusion as a displayed cell.
- [ ] Empty declared group/level and zero-row denominators return integer zero plus numeric `NA` without warnings.
- [ ] Standard and declared-code missing values combine into the missing row without not-applicable inference.
- [ ] Canonical row basis is rejected; invalid basis and malformed/corrupt aggregate components fail value-free validation.

## Consumer And Compatibility Tests

- [ ] Default Table 1 retains its exact previous form, output schema, ordering and display strings; all categorical denominator/proportion values trace to the shared calculation.
- [ ] Opt-in Table 1 bases change only categorical denominator/display/note fields and preserve non-categorical rows.
- [ ] Data-frame and PostgreSQL frequency companions retain the existing four leading fields and exact count/collapse behaviour, add the shared fields, and reconcile `count == numerator` and collapsed proportions.
- [ ] Plot objects retain count axes and current category/missing behaviour.
- [ ] `epi_eda_run()` and intake add one aggregate display component/artifact while preserving existing components, summary schemas, manifest columns and checksums.
- [ ] Data-frame and intake reports render literal fixture numerator/denominator/proportion cells from their saved/current display tables.

## PostgreSQL And Bundle Tests

- [ ] Categorical companion enrichment invokes no new database fetch/query and remains inside the existing snapshot path.
- [ ] Default flat bundle result/file/metadata schemas remain exact and contain no companion artifact.
- [ ] New delivery frequency CSVs contain the shared fields, use `compact-plot-data-2`, remain manifest-owned and reconcile to canonical summaries.
- [ ] The renderer accepts a valid legacy four-column `compact-plot-data-1` frequency companion and enriches it in memory.
- [ ] New malformed, inconsistent or tampered companion data fail before report publication and preserve the prior bundle byte-for-byte.
- [ ] Rendered database HTML contains the literal companion values and no source observations, SQL, credentials or new absolute/network assets.

## Verification

- [ ] Focused calculation, Table 1, plot, runner, intake and report suites pass.
- [ ] Live PostgreSQL tests prove query/timing parity and inspect the exact delivery CSV/HTML artifacts.
- [ ] Changed R files are styled and package lint is clean.
- [ ] `scripts/check-local.sh` passes.
- [ ] `scripts/check-cran.sh` passes with only reconciled inherited external notes, if any.
- [ ] Representative frequency tables, SVGs and both HTML report families are rendered and visually inspected.
- [ ] Required GitHub checks are green with no actionable review feedback.

## Baseline Evidence

At canonical `master` `26fabdf`, the focused canonical summary, stratified, Table 1 and PostgreSQL source suites pass. Current Table 1 has only a `result` formal and compact empty frequency data have exactly `level`, `count`, `display_order` and `remainder`.
