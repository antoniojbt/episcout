% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_geo.R
\name{epi_geo_from_coords}
\alias{epi_geo_from_coords}
\title{Convert reviewed coordinate columns to simple features}
\usage{
epi_geo_from_coords(data, x, y, crs, remove = FALSE)
}
\arguments{
\item{data}{A data frame with unique column names.}

\item{x, y}{Exact distinct names of numeric coordinate columns, in x/y or
longitude/latitude order as established by the reviewed data dictionary.}

\item{crs}{One EPSG integer or character CRS value resolvable by \code{\link[sf:st_crs]{sf::st_crs()}}.}

\item{remove}{Whether to remove the source coordinate columns after a
successful conversion. Defaults to \code{FALSE} for auditability.}
}
\value{
A list with \code{data}, \code{audit} and \code{metadata}. \code{data} is an \code{sf} object
only when every row has a complete finite pair (and an in-range pair for
EPSG:4326); otherwise it is \code{NULL}. The other components contain aggregate
counts and reviewed metadata, never coordinate values.
}
\description{
Audit an explicitly selected numeric x/y pair and convert the complete data
frame to point geometry only when every row is eligible.
}
\details{
CRS assignment gives existing values their reviewed meaning; it is
not a coordinate transformation. Missing, non-finite and out-of-range rows
block all conversion and are never dropped silently.
}
