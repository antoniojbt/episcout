% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_categorical_display.R
\name{epi_eda_categorical_display}
\alias{epi_eda_categorical_display}
\title{Calculate categorical display denominators}
\usage{
epi_eda_categorical_display(
  result,
  basis = c("compatibility", "column", "row", "overall")
)
}
\arguments{
\item{result}{A canonical summary list returned by
\code{\link[=epi_eda_profile_summaries]{epi_eda_profile_summaries()}} or an \code{epi_eda_stratified} result returned
by \code{\link[=epi_eda_profile_stratified]{epi_eda_profile_stratified()}}.}

\item{basis}{Percentage basis. \code{"compatibility"} uses observed values for
ordinary levels and all group rows for the missing level. \code{"column"}
uses all rows in each group. \code{"row"} distributes each level across
non-Overall strata and is available only for stratified results.
\code{"overall"} uses the included analysis population.}
}
\value{
A data frame with fixed columns \code{variable_order}, \code{level_order},
\code{name}, \code{label}, \code{type}, \code{level}, \code{group_id}, \code{group_order},
\code{group_label}, \code{is_overall}, \code{group_n}, \code{population_n}, \code{numerator},
\code{denominator}, \code{proportion}, \code{percentage_basis}, \code{denominator_scope},
\code{missing_treatment}, and \code{is_missing_level}. A zero denominator is
retained as zero and its proportion is \code{NA_real_}.
}
\description{
Derive one traceable categorical numerator, denominator, and proportion
table from existing canonical or stratified EDA aggregate components. The
function never reads source observations or opens a database connection.
}
\details{
Standard missing values and declared missing codes are combined in
one explicit missing-level row. The function does not infer a
not-applicable category or preferred percentage basis. Row denominators
exclude Overall from the cross-stratum sum to avoid double-counting.
}
