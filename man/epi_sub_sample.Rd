% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sub_sampling.R
\name{epi_sub_sample}
\alias{epi_sub_sample}
\title{Sub-sample a data frame balancing a binary outcome}
\usage{
epi_sub_sample(df, outcome, proportion = 0.1)
}
\arguments{
\item{df}{Data frame containing the data to be sampled.}

\item{outcome}{Name of the binary outcome column.}

\item{proportion}{Proportion of rows to sample (0 < proportion <= 1).}
}
\value{
A data frame containing the sampled rows.
}
\description{
Draw a sub-sample without replacement from a data frame ensuring an
approximately balanced binary outcome distribution.
}
\examples{
set.seed(123)
df <- data.frame(
  outcome = rbinom(1000, 1, 0.7),
  value = rnorm(1000)
)
sub_df <- epi_sub_sample(df, "outcome", proportion = 0.1)
table(sub_df$outcome)
}
