% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_linkage_spec.R
\name{epi_linkage_text_profile}
\alias{epi_linkage_text_profile}
\title{Declare a text normalisation profile for record linkage}
\usage{
epi_linkage_text_profile(
  unicode = "NFC",
  case = "fold",
  diacritics = "keep",
  punctuation = "space",
  whitespace = "collapse",
  token_order = "preserve",
  drop_tokens = character()
)
}
\arguments{
\item{unicode}{Unicode normalisation form, either \code{"NFC"} or \code{"NFKC"}.}

\item{case}{Either \code{"keep"} or \code{"fold"}.}

\item{diacritics}{Either \code{"keep"} or \code{"strip"}.}

\item{punctuation}{One of \code{"keep"}, \code{"space"} or \code{"drop"}.}

\item{whitespace}{One of \code{"keep"}, \code{"trim"} or \code{"collapse"}.}

\item{token_order}{Either \code{"preserve"} or \code{"sort"}.}

\item{drop_tokens}{Explicit tokens to remove after applying the other profile
operations. The default removes nothing.}
}
\value{
An \code{epi_linkage_text_profile} object.
}
\description{
Creates an explicit profile used to derive comparison representations without
changing source values. The profile is generic: no particle or name component
is removed or interpreted unless the caller declares it.
}
