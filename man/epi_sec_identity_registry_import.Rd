% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_sec_registry_import.R
\name{epi_sec_identity_registry_import}
\alias{epi_sec_identity_registry_import}
\title{Import reviewed assignments into an identity registry}
\usage{
epi_sec_identity_registry_import(
  con,
  registry_schema,
  source_schema,
  source_table,
  identifier_column,
  token_column,
  identity_namespace,
  normalization = c("identity", "trim", "trim_upper"),
  validity_regex = NULL,
  invalid_policy = c("fail", "retain_and_flag"),
  mode = c("audit", "apply")
)
}
\arguments{
\item{con}{An open PostgreSQL DBI connection created with RPostgres.}

\item{registry_schema}{A compatible version-2 identity-registry schema.}

\item{source_schema, source_table}{Existing ordinary PostgreSQL source relation.}

\item{identifier_column, token_column}{Source columns containing identifiers and preserved tokens.}

\item{identity_namespace}{Non-empty registry namespace for the identifiers.}

\item{normalization}{\code{"identity"}, \code{"trim"} or \code{"trim_upper"}.}

\item{validity_regex}{\code{NULL} or a non-empty PostgreSQL regular expression applied after preparation.}

\item{invalid_policy}{\code{"fail"} or \code{"retain_and_flag"}; import records no flag, but the latter permits reviewed regex mismatches.}

\item{mode}{\code{"audit"} performs no writes; \code{"apply"} repeats the audit and imports atomically.}
}
\value{
An \code{epi_sec_registry_import_result} with status, mode, writes, aggregate counts and value-free issues.
}
\description{
Audit or transactionally import a PostgreSQL relation containing existing identifier-to-token assignments. Identifier and token values remain in PostgreSQL.
}
\details{
Import never changes an existing alias assignment. Blank identifiers or tokens, preparation collisions, duplicate source mappings and registry conflicts prevent every write. Multiple identifiers may intentionally share one token. The function does not grant privileges, select roles, copy values into R or manage backups.
}
\seealso{
Other longitudinal pseudonymisation:
\code{\link[=epi_sec_alias_db]{epi_sec_alias_db()}},
\code{\link[=epi_sec_alias_spec]{epi_sec_alias_spec()}},
\code{\link[=epi_sec_identifier_spec]{epi_sec_identifier_spec()}},
\code{\link[=epi_sec_identity_registry_init]{epi_sec_identity_registry_init()}},
\code{\link[=epi_sec_identity_universe_db]{epi_sec_identity_universe_db()}},
\code{\link[=epi_sec_identity_universe_spec]{epi_sec_identity_universe_spec()}},
\code{\link[=epi_sec_linkage_scaffold]{epi_sec_linkage_scaffold()}},
\code{\link[=epi_sec_linkage_spec]{epi_sec_linkage_spec()}},
\code{\link[=epi_sec_pseudonym]{epi_sec_pseudonym()}},
\code{\link[=epi_sec_pseudonymise_db]{epi_sec_pseudonymise_db()}}
}
\concept{longitudinal pseudonymisation}
