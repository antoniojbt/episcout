% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_linkage_score.R
\name{epi_linkage_validate}
\alias{epi_linkage_validate}
\title{Validate linkage candidates and decisions against complete truth}
\usage{
epi_linkage_validate(result, truth)
}
\arguments{
\item{result}{An \code{\link[=epi_linkage_classify]{epi_linkage_classify()}} or \code{\link[=epi_linkage_run]{epi_linkage_run()}} result.}

\item{truth}{A complete data frame with exact columns \code{x_index}, \code{y_index},
\code{is_match} and one unique row for every possible pair.}
}
\value{
An aggregate-only \code{epi_linkage_validation} object containing explicit
numerator, denominator and measure columns. No truth or source values are
retained.
}
\description{
Evaluates blocking separately from final decisions. V1 requires one truth row
for every Cartesian pair so that non-candidate pairs are known rather than
silently assumed to be non-matches.
}
