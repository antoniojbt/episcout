% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_geo_postgis.R
\name{epi_geo_postgis_collect}
\alias{epi_geo_postgis_collect}
\title{Collect an explicitly bounded PostGIS selection}
\usage{
epi_geo_postgis_collect(
  source,
  columns = character(),
  bbox = NULL,
  max_features = 10000L
)
}
\arguments{
\item{source}{An unmodified \code{\link[=epi_geo_postgis_source]{epi_geo_postgis_source()}} object.}

\item{columns}{A unique character allow-list of exact non-geometry column
identifiers. Defaults to geometry only.}

\item{bbox}{\code{NULL} for the complete relation, or an \code{\link[sf:st_bbox]{sf::st_bbox()}} object
with finite ordered bounds in the resolved source CRS.}

\item{max_features}{One positive whole-number upper bound. The function
refuses an over-limit selection and never truncates it.}
}
\value{
An \code{sf} object satisfying the Phase-A XY geometry and CRS contract.
}
\description{
Materialise only a reviewed allow-list of ordinary columns plus geometry,
and only when the complete selection fits within a positive feature bound.
}
\details{
This is the only PostGIS path that collects feature geometry or
attributes. Identifiers are quoted, bbox ordinates are bound as query
values, and counting and collection occur in one read-only repeatable-read
snapshot. Row order is retained as returned by PostgreSQL but is not a
stable ordering contract. The caller owns and must close the connection.
}
