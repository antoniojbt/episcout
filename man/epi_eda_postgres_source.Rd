% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_postgres_source.R
\name{epi_eda_postgres_source}
\alias{epi_eda_postgres_source}
\title{Reference a PostgreSQL relation for specification-first EDA}
\usage{
epi_eda_postgres_source(con, schema, relation)
}
\arguments{
\item{con}{An open, idle connection created by RPostgres.}

\item{schema}{One PostgreSQL schema name. Dotted names and SQL fragments are
not accepted.}

\item{relation}{One table or view name in \code{schema}.}
}
\value{
An \code{epi_eda_postgres_source} list with fixed components \code{con},
\code{schema}, \code{relation}, \code{relation_kind}, \code{columns}, and \code{source_version}.
Printing and structure display redact the connection and relation names.
}
\description{
Validate a caller-owned RPostgres connection and one schema-qualified
relation without collecting observations. The returned object can be passed
to the four \verb{epi_eda_*} profiling functions and \code{\link[=epi_eda_db_run]{epi_eda_db_run()}}.
}
\details{
PostgreSQL 17 or later is required. Ordinary tables, partitioned
tables, views, materialized views, and foreign tables are supported.
Temporary and other relation kinds are rejected. The connection remains
caller-owned and must remain open and idle while the source is used.
}
