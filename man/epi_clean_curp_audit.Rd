% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_clean_curp.R
\name{epi_clean_curp_audit}
\alias{epi_clean_curp_audit}
\title{Audit CURP structure and reconcile reviewed reference fields}
\usage{
epi_clean_curp_audit(
  curp,
  birth_date = NULL,
  sex_code = NULL,
  birthplace_code = NULL,
  initials = NULL
)
}
\arguments{
\item{curp}{A character vector. Values are checked exactly as supplied;
lowercase, whitespace and Unicode are not normalised.}

\item{birth_date}{An optional \code{Date} vector of length one or \code{length(curp)}.}

\item{sex_code}{An optional character vector containing CURP recorded-sex
codes \code{H}, \code{M} or missing values.}

\item{birthplace_code}{An optional character vector containing reviewed CURP
birthplace codes or missing values.}

\item{initials}{An optional character vector containing reviewed four-letter
CURP initials segments or missing values. Names are not accepted or derived.}
}
\value{
An \code{epi_curp_audit} list containing \code{records}, \code{issues}, \code{comparison}
and aggregate \code{summary} tibbles. \code{records} omits the original CURP but its
derived date, recorded-sex code, birthplace and initials remain restricted
personal data. \code{valid} means only that the documented local structural
checks passed. \code{checksum_status} is \code{not_verified} because the authoritative
position-18 algorithm is not publicly evidenced for this implementation.
}
\description{
Performs local, vector-safe structural checks for CURP values and optionally
compares successfully derived fields with separately reviewed reference
vectors. It makes no network calls and does not return the supplied CURP.
}
\details{
Numeric position-17 markers are interpreted within the supported
1900--1999 domain and \code{A}--\code{J} markers within 2000--2099. A possible pre-1900
key cannot be distinguished from the corresponding 1900s key locally. Future
dates are rejected. Local structure does not prove registry assignment,
certification, authenticity or identity. Callers control access, storage,
retention and linkage of the restricted row-level result.
}
\examples{
epi_clean_curp_audit(c(NA_character_, "NOT-A-CURP"))

}
