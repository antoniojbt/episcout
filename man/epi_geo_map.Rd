% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_geo.R
\name{epi_geo_map}
\alias{epi_geo_map}
\title{Create a reviewed static simple-feature map}
\usage{
epi_geo_map(
  x,
  value = NULL,
  geometry_colour = NULL,
  geometry_fill = NULL,
  na_colour = "grey80"
)
}
\arguments{
\item{x}{A reviewed \code{sf} object supported by the phase-A spatial contract.}

\item{value}{\code{NULL} for geometry only, or one exact non-geometry attribute
name containing numeric, character, factor or logical values.}

\item{geometry_colour, geometry_fill}{Optional constant geometry colours.
A constant cannot be supplied for the aesthetic mapped by \code{value}.}

\item{na_colour}{One colour used for missing mapped values.}
}
\value{
An ordinary \code{ggplot} object containing one simple-feature layer.
}
\description{
Create a geometry-only map or map one numeric or categorical attribute with
the existing \code{\link[ggplot2:geom_sf]{ggplot2::geom_sf()}} path. The returned object can be extended
with ordinary \code{ggplot2} layers, scales and themes.
}
\details{
Polygon value maps use fill; point and line value maps use colour.
Mixed polygonal and non-polygonal objects require explicit user-composed
\code{ggplot2} layers. The function does not add a basemap, classify continuous
values, suppress locations or make a disclosure-safety claim.
}
