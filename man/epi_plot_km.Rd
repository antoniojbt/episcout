% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/KM_plot.R
\name{epi_plot_km}
\alias{epi_plot_km}
\title{Plot Kaplan-Meier survival curves}
\usage{
epi_plot_km(survfit_obj)
}
\arguments{
\item{survfit_obj}{A \code{survival::survfit} object.}
}
\value{
A \code{ggplot2} object. The data used to build the plot is attached as
an attribute named \code{surv_data}.
}
\description{
Convert a \code{survival::survfit} object into a tidy data frame and plot the
survival curves with \code{ggplot2}.
}
\examples{
if (requireNamespace("survival", quietly = TRUE) &&
  requireNamespace("ggplot2", quietly = TRUE)) {
  fit <- survival::survfit(survival::Surv(time, status) ~ sex,
    data = survival::lung
  )
  epi_plot_km(fit)
}
}
