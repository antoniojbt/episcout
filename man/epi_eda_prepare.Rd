% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_prepare.R
\name{epi_eda_prepare}
\alias{epi_eda_prepare}
\title{Prepare data using a reviewed EDA specification}
\usage{
epi_eda_prepare(
  data,
  spec,
  mode = c("audit", "apply"),
  unexpected_levels = c("error", "append"),
  extra_variables = c("keep", "error", "drop")
)
}
\arguments{
\item{data}{A data frame to assess or prepare.}

\item{spec}{An EDA specification data frame or CSV path accepted by
\code{\link[=epi_eda_spec]{epi_eda_spec()}}. If scaffold evidence includes \code{review_status}, every row
must be \code{reviewed} before apply mode can proceed.}

\item{mode}{Either \code{"audit"} (the default) or \code{"apply"}.}

\item{unexpected_levels}{Either \code{"error"} (the default) or \code{"append"} for
categorical variables. Binary variables never append a third level.}

\item{extra_variables}{Either \code{"keep"} (the default), \code{"error"}, or
\code{"drop"}.}
}
\value{
An \code{epi_eda_preparation} list with fixed components \code{data}, \code{audit},
\code{schema_before}, \code{schema_after}, and \code{metadata}. Audits contain names,
classes and counts but no observed values. \code{schema_after} is non-\code{NULL}
only after a successful apply.
}
\description{
Audit or apply deterministic, specification-guided missing-value, type and
level preparation. Audit mode never changes the returned data. Apply mode is
all-or-nothing: any blocking finding returns the original data and no
after-schema.
}
\details{
Character numeric parsing is deliberately unsupported because no
locale or decimal-mark contract is present. Character dates and datetimes
use strict ISO forms. Offset or \code{Z} datetimes are normalised to UTC; local
datetimes require a valid reviewed \code{timezone} field and ambiguous or
nonexistent daylight-saving wall times block preparation. Optional \code{min}
and \code{max} fields are descriptive metadata, not recoding rules.

The semicolon-delimited v1 \code{missing_codes} format cannot represent empty,
whitespace-only, or semicolon-containing sentinels. No files are written.
}
