% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_association_cramers_v.R
\name{epi_eda_cramers_v}
\alias{epi_eda_cramers_v}
\title{Calculate descriptive Cramer's V from aggregate counts}
\usage{
epi_eda_cramers_v(counts)
}
\arguments{
\item{counts}{A data frame containing character columns \code{left}, \code{right},
\code{row_level} and \code{column_level}, plus numeric non-negative whole-number
\code{n}. Pair order is the order of first occurrence. Missing combinations of
supplied row and column levels are treated as zero-count cells; explicit
zero rows retain otherwise inactive domain evidence.}
}
\value{
A data frame with \code{left}, \code{right}, total \code{n}, \code{active_rows},
\code{active_columns}, \code{cramers_v}, \code{status} and \code{reason}. Zero total or fewer
than two active rows or columns returns typed unavailable evidence.
}
\description{
Calculate Cramer's V for one or more explicit variable pairs from long-form
aggregate contingency counts. The calculation removes zero-total margins
from the active dimensions and uses Pearson expectations without continuity
or small-sample bias correction.
}
\details{
Counts and their pair totals must be exactly representable as base-R
doubles from zero through \code{2^53 - 1}. Each explicit pair/row/column cell
must occur at most once. This function consumes aggregates only and
calculates no p-value, threshold, strength label or interpretation.

The categorical component from \code{\link[=epi_eda_profile_stratified]{epi_eda_profile_stratified()}} can be
adapted without another source query: use its \code{group_value}, \code{name},
\code{level} and \code{n} fields as \code{row_level}, \code{right}, \code{column_level} and \code{n}, and
repeat the stratifier name from result metadata as \code{left}. Exclude Overall
and missing-level rows when constructing that ordinary contingency table.
}
