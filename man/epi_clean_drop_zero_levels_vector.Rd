% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_clean_drop_zero_levels.R
\name{epi_clean_drop_zero_levels_vector}
\alias{epi_clean_drop_zero_levels_vector}
\title{Drop Unused Levels from a Factor Vector}
\usage{
epi_clean_drop_zero_levels_vector(factor_var)
}
\arguments{
\item{factor_var}{A factor vector in which you want to drop unused levels.}
}
\value{
A factor vector with all levels that have zero observations removed.
}
\description{
Removes any factor levels that have zero occurrences, which can arise
after introducing \code{NA} values or subsetting. Handy for cleaning up
factor vectors before analysis or modeling.
}
\examples{
\dontrun{
# Single vector example
factor_var <- factor(c("a", "b", "a", "c", "b", "a", "b", "c"))
factor_var[c(2, 5)] <- NA # simulate missing values that remove counts
cleaned_factor_var <- epi_clean_drop_zero_levels_vector(factor_var)
print(cleaned_factor_var)

# Data frame example: apply to each factor column
df <- data.frame(
  col1 = factor(c("a", "b", "a", "c", "b")),
  col2 = factor(c("d", "d", "e", "f", "d")),
  col3 = factor(c("g", "h", "g", "g", "h"))
)
df$col1[c(2, 5)] <- NA
df$col2[4] <- NA
df$col3[5] <- NA
df[] <- lapply(df, function(column) {
  if (is.factor(column)) {
    epi_clean_drop_zero_levels_vector(column)
  } else {
    column
  }
})
print(df)
}
}
