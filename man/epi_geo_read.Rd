% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_geo.R
\name{epi_geo_read}
\alias{epi_geo_read}
\title{Read a reviewed local vector layer}
\usage{
epi_geo_read(dsn, layer = NULL)
}
\arguments{
\item{dsn}{One existing local \code{.gpkg} or \code{.shp} path. URLs, GDAL virtual
file-system paths and database connections are not accepted.}

\item{layer}{\code{NULL} for an unambiguous dataset, or one exact spatial layer
name.}
}
\value{
An \code{sf} object with feature order, attributes, geometry and CRS
retained from the selected layer.
}
\description{
Read one spatial layer from a local GeoPackage or Shapefile into an
\link[sf:sf]{sf::sf} object. A GeoPackage with more than one spatial layer requires an
explicit layer selection. Shapefile input requires its \code{.shp}, \code{.shx} and
\code{.dbf} files to remain together.
}
\details{
Inputs must have one active XY geometry column, a non-missing CRS
and supported point, line or polygon geometry. Shapefile is an import
compatibility format and may have driver-dependent field-name, encoding
and geometry limitations; GeoPackage is preferred.
}
