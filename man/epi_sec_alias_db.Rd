% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_sec_alias_resolution.R
\name{epi_sec_alias_db}
\alias{epi_sec_alias_db}
\title{Audit or materialise exact identifier aliases}
\usage{
epi_sec_alias_db(
  con,
  spec,
  mode = c("audit", "materialise"),
  output_schema = NULL,
  output_table = NULL,
  existing = "error",
  statement_timeout = 60,
  lock_timeout = 30
)
}
\arguments{
\item{con}{An open, idle PostgreSQL DBI connection.}

\item{spec}{An unmodified object returned by \code{\link[=epi_sec_alias_spec]{epi_sec_alias_spec()}}.}

\item{mode}{Either \code{"audit"} or \code{"materialise"}.}

\item{output_schema}{Existing destination schema for materialisation.}

\item{output_table}{New ordinary PostgreSQL destination table.}

\item{existing}{Destination policy; only \code{"error"} is supported.}

\item{statement_timeout}{Maximum seconds for each statement, from 1 to 3600.}

\item{lock_timeout}{Maximum seconds for the destination advisory lock, from
1 to 3600.}
}
\value{
An \code{epi_sec_alias_result} with value-free source and mapping audits,
issues, and crosswalk metadata suitable for \code{\link[=epi_sec_linkage_spec]{epi_sec_linkage_spec()}}.
}
\description{
Audit explicit identifier-pair declarations in one PostgreSQL snapshot, or
materialise a flat \code{alias_id}/\code{canonical_id} crosswalk after all ambiguity
checks pass. Ordinary result objects are aggregate and value-free.
}
\details{
Source rows with a missing, blank, or regex-invalid declared
identifier are errors.  Alias-to-many-canonical and canonical-to-many-alias
mappings are errors; duplicate exact pairs are reported as warnings.  Apply
never replaces a destination, and rolls back fully if checks change between
preflight and write.  The resulting table contains no namespace columns:
namespaces remain caller-declared linkage metadata.
}
\seealso{
Other longitudinal pseudonymisation:
\code{\link[=epi_sec_alias_spec]{epi_sec_alias_spec()}},
\code{\link[=epi_sec_identity_registry_init]{epi_sec_identity_registry_init()}},
\code{\link[=epi_sec_identity_universe_db]{epi_sec_identity_universe_db()}},
\code{\link[=epi_sec_identity_universe_spec]{epi_sec_identity_universe_spec()}},
\code{\link[=epi_sec_linkage_scaffold]{epi_sec_linkage_scaffold()}},
\code{\link[=epi_sec_linkage_spec]{epi_sec_linkage_spec()}},
\code{\link[=epi_sec_pseudonym]{epi_sec_pseudonym()}},
\code{\link[=epi_sec_pseudonymise_db]{epi_sec_pseudonymise_db()}}
}
\concept{longitudinal pseudonymisation}
