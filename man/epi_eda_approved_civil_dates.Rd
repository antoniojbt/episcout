% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_civil_dates.R
\name{epi_eda_approved_civil_dates}
\alias{epi_eda_approved_civil_dates}
\title{Validate analyst-approved civil-date operations}
\usage{
epi_eda_approved_civil_dates(operations)
}
\arguments{
\item{operations}{A non-empty data frame containing exactly
\code{source_variable_key}, \code{derived_name}, \code{operation_state},
\code{declared_semantics}, \code{preserve_source}, \code{require_midnight}, and
\code{approval_id} in that order. See Details.}
}
\value{
An \code{epi_eda_approved_civil_dates} data frame in canonical opaque-key
order.
}
\description{
Create the executable operation object consumed by
\code{\link[=epi_eda_derive_civil_dates]{epi_eda_derive_civil_dates()}}. Civil-date meaning must come from external
analyst review; episcout never infers it from a column name, source storage
or observed midnight values.
}
\details{
\code{operation_state} must be \code{"approved"}, \code{declared_semantics} must be
\code{"civil_date"}, and both safeguards must be non-missing \code{TRUE} values.
Source keys must be unique caller-managed opaque keys matching
\verb{^var_[a-z0-9]\{16,64\}$}; derived names must be unique non-empty valid UTF-8
text. \code{approval_id} is a caller-managed opaque reference matching
\verb{^approval_[a-z0-9]\{16,64\}$}. Episcout validates the declared state and
structure but does not authenticate the approving analyst.

Extra or reordered fields, pending operations, undeclared semantics, false
safeguards, duplicate keys or derived names, and malformed approval
references are rejected. Printing and structure display omit source and
derived identities and approval references.
}
