% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_relationship_profile.R
\name{epi_db_relationship_profile}
\alias{epi_db_relationship_profile}
\title{Profile explicit PostgreSQL code and label relationships}
\usage{
epi_db_relationship_profile(con, dictionary, pairs, max_levels = 500)
}
\arguments{
\item{con}{An open PostgreSQL-compatible DBI connection.}

\item{dictionary}{A validated extended dictionary.}

\item{pairs}{A data frame containing exactly \code{left_schema}, \code{left_table},
\code{left_column}, \code{right_schema}, \code{right_table} and \code{right_column}, in that
order. Reversed pairs are separate requests.}

\item{max_levels}{Maximum distinct non-NULL \verb{(left, right)} combinations
allowed for each pair.}
}
\value{
An \code{epi_db_relationship_profile} list containing exactly \code{summary},
\code{mappings} and \code{conflicts}. All components start with the six pair keys.
\code{summary} then contains \code{total_rows}, \code{both_present}, \code{left_missing},
\code{right_missing}, \code{both_missing}, \code{distinct_left},
\code{distinct_right}, \code{distinct_combinations}, \code{max_right_per_left},
\code{max_left_per_right}, \code{left_values_with_multiple_right},
\code{right_values_with_multiple_left} and \code{relationship_class}. \code{mappings} adds
character \code{left_value} and \code{right_value}, and aggregate \code{n}. \code{conflicts}
adds \code{exception_type}, character \code{left_value} and \code{right_value}, and
aggregate \code{n}. Empty selections return typed zero-row components.
}
\description{
Inspect bounded aggregate relationships between explicitly selected pairs of
active dictionary columns from the same PostgreSQL table.
}
\details{
Only active dictionary columns whose recorded \code{source_data_type} is
an explicitly supported PostgreSQL scalar type can be profiled. Every pair
is preflighted before any detail query; if its distinct both-present
combination count exceeds \code{max_levels}, the complete call fails without a
partial result. SQL NULL is partitioned separately and never becomes a
mapping value. Literal observed values such as empty text, \code{"NA"} and
\code{"NULL"} remain ordinary values.

\code{relationship_class} is one of \code{insufficient_data}, \code{one_to_one},
\code{constant_left}, \code{constant_right}, \code{one_to_many}, \code{many_to_one} or
\code{many_to_many}. Directional conflict types are
\code{left_maps_multiple_right} and \code{right_maps_multiple_left}; missingness types
are \code{left_missing}, \code{right_missing} and \code{both_missing}. Missingness
conflict values are \code{NA_character_}. These technical classes do not
declare equivalence, redundancy, a canonical field or a transformation.
Queries are aggregate-only, quote identifiers through DBI, cast returned
mapping values to PostgreSQL \code{text}, and do not manage transactions or
modify the database.
}
\seealso{
\code{\link[=epi_db_catalogue_profile]{epi_db_catalogue_profile()}}, \code{\link[=epi_eda_dictionary_scaffold]{epi_eda_dictionary_scaffold()}},
\code{\link[=epi_eda_dictionary_validate]{epi_eda_dictionary_validate()}}
}
