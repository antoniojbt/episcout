% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_sec_identifier_spec.R
\name{epi_sec_identifier_spec}
\alias{epi_sec_identifier_spec}
\title{Define identifier preparation for PostgreSQL pseudonymisation}
\usage{
epi_sec_identifier_spec(linkage, rules = NULL)
}
\arguments{
\item{linkage}{An object returned by \code{\link[=epi_sec_linkage_spec]{epi_sec_linkage_spec()}}.}

\item{rules}{\code{NULL} for exact identity preparation and fail-on-invalid behaviour, or a data frame with exactly \code{source_schema}, \code{source_table}, \code{normalization}, \code{validity_regex}, \code{invalid_policy} and \code{validity_column}.}
}
\value{
An \code{epi_sec_identifier_spec} containing sorted metadata-only \code{rules}, \code{contract_version} and \code{fingerprint_sha256}.
}
\description{
Create a value-free contract describing how each linked identifier is prepared and validated before registry matching.
}
\details{
Every linkage source table has exactly one rule. \code{normalization} is \code{identity}, \code{trim} or \code{trim_upper}. \code{validity_regex} is \code{NA} when no regular expression applies. \code{invalid_policy} is \code{fail} or \code{retain_and_flag}; the latter requires a non-empty \code{validity_column}, while \code{fail} requires \code{NA}. Omitting \code{rules} preserves released byte-exact matching behaviour.
}
\seealso{
Other longitudinal pseudonymisation:
\code{\link[=epi_sec_alias_db]{epi_sec_alias_db()}},
\code{\link[=epi_sec_alias_spec]{epi_sec_alias_spec()}},
\code{\link[=epi_sec_identity_registry_import]{epi_sec_identity_registry_import()}},
\code{\link[=epi_sec_identity_registry_init]{epi_sec_identity_registry_init()}},
\code{\link[=epi_sec_identity_universe_db]{epi_sec_identity_universe_db()}},
\code{\link[=epi_sec_identity_universe_spec]{epi_sec_identity_universe_spec()}},
\code{\link[=epi_sec_linkage_scaffold]{epi_sec_linkage_scaffold()}},
\code{\link[=epi_sec_linkage_spec]{epi_sec_linkage_spec()}},
\code{\link[=epi_sec_pseudonym]{epi_sec_pseudonym()}},
\code{\link[=epi_sec_pseudonymise_db]{epi_sec_pseudonymise_db()}}
}
\concept{longitudinal pseudonymisation}
