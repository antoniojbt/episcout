% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_cleaning.R
\name{epi_eda_approved_rules}
\alias{epi_eda_approved_rules}
\title{Validate analyst-approved cleaning rules}
\usage{
epi_eda_approved_rules(rules)
}
\arguments{
\item{rules}{A non-empty data frame containing exactly \code{variable_key},
\code{rule_state}, \code{declared_type}, \code{valid_min}, \code{valid_max}, \code{allowed_values},
\code{missing_codes}, and \code{approval_id} in that order. See Details.}
}
\value{
An \code{epi_eda_approved_rules} data frame in canonical opaque-key order.
}
\description{
Create the executable rule object consumed by
\code{\link[=epi_eda_apply_cleaning_rules]{epi_eda_apply_cleaning_rules()}}. This schema is separate from descriptive
EDA dictionaries and the pending result returned by
\code{\link[=epi_eda_qc_proposals]{epi_eda_qc_proposals()}}.
}
\details{
\code{rule_state} must be \code{"approved"}. Supported declared types are
\code{numeric}, \code{integer}, \code{categorical}, and \code{binary}. Numeric and integer rules
may contain finite \code{valid_min}/\code{valid_max} bounds and approved semicolon-
delimited \code{missing_codes}; categorical and binary rules may contain exact
semicolon-delimited \code{allowed_values} and \code{missing_codes}. Every row must
define at least one operation. A populated binary allowed set contains
exactly two values. Allowed and missing sets must not overlap.

\code{variable_key} must match \verb{^var_[a-z0-9]\{16,64\}$}. \code{approval_id} is a
caller-managed opaque reference matching
\verb{^approval_[a-z0-9]\{16,64\}$}; episcout validates the declared state and
structure but does not authenticate the approving analyst. Pending
proposal objects, descriptive dictionary bounds, unsupported types,
contradictory bounds, non-finite numeric instructions, duplicate keys,
empty list tokens, and extra or reordered fields are rejected.
}
