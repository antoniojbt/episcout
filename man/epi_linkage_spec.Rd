% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_linkage_spec.R
\name{epi_linkage_spec}
\alias{epi_linkage_spec}
\title{Declare a bounded probabilistic record-linkage workflow}
\usage{
epi_linkage_spec(
  x_id,
  y_id,
  profiles = list(),
  blocks,
  comparisons,
  max_candidates,
  model = NULL,
  thresholds = NULL
)
}
\arguments{
\item{x_id, y_id}{Exact record-key column names in the two source data frames.}

\item{profiles}{A uniquely named list created with
\code{\link[=epi_linkage_text_profile]{epi_linkage_text_profile()}}. Built-in profile \code{"identity"} is always
available and must not be supplied.}

\item{blocks}{Data frame with columns \code{pass}, \code{x_field}, \code{y_field}, \code{profile}.}

\item{comparisons}{Data frame with columns \code{comparison}, \code{x_field},
\code{y_field}, \code{profile}, \code{method}, \code{parameter}.}

\item{max_candidates}{Explicit hard candidate cap, from 1 through 10,000,000.}

\item{model}{Optional list with \code{parameters} and \code{match_prevalence}; see
Details.}

\item{thresholds}{Optional list with \code{metric}, \code{nonmatch_max} and \code{match_min}.}
}
\value{
An \code{epi_linkage_spec} object.
}
\description{
Defines source record-key columns, derived normalisation profiles, exact
blocking passes, field comparisons, a hard candidate cap and optional
Fellegi-Sunter model/decision parameters. No role or threshold is inferred.
}
\details{
Rows in one blocking \code{pass} are joined with AND; distinct passes are
combined with OR. Candidate overflow fails rather than truncates. Supported
comparison methods are \code{exact}, \code{jaro_winkler}, \code{token_jaccard},
\code{numeric_tolerance} and \code{date_tolerance}.

\code{model$parameters} must contain \code{comparison}, \code{m_probability} and
\code{u_probability} in comparison order, with \verb{0 < u < m < 1}. A model posterior
is not an empirical calibration. The package supplies no model values or
decision thresholds.
}
