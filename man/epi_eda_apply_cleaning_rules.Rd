% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_cleaning.R
\name{epi_eda_apply_cleaning_rules}
\alias{epi_eda_apply_cleaning_rules}
\title{Apply approved cleaning rules and materialise a complete processed output}
\usage{
epi_eda_apply_cleaning_rules(
  data,
  rules,
  variable_keys,
  output_path = NULL,
  output_format = NULL,
  destination_schema = NULL,
  destination_table = NULL
)
}
\arguments{
\item{data}{A data frame or an \code{\link[=epi_eda_postgres_source]{epi_eda_postgres_source()}}.}

\item{rules}{An unmodified object returned by \code{\link[=epi_eda_approved_rules]{epi_eda_approved_rules()}}.}

\item{variable_keys}{A caller-owned data frame containing exactly \code{name} and
\code{variable_key}. Every approved key must resolve exactly once. Additional
unruled mappings are permitted.}

\item{output_path}{For a data-frame source, an optional new destination file.}

\item{output_format}{When \code{output_path} is supplied, exactly \code{"csv"} or
\code{"rds"}. The format is never inferred. RData and Parquet are unsupported.}

\item{destination_schema}{For a PostgreSQL source, the existing schema in
which to create the new destination table.}

\item{destination_table}{For a PostgreSQL source, the new table name.}
}
\value{
An \code{epi_eda_cleaning_result} list with \code{data} and \code{audit}. For a
data-frame source, \code{data} is the complete processed data frame, including
when it was also exported. For PostgreSQL, \code{data} is \code{NULL} because rows
remain server-side. \code{audit} contains one aggregate \code{summary} row and an
opaque-keyed \code{variables} table with missing counts before and after,
observed-to-missing transition counts, dimensions, a deterministic rule
hash, and reconciliation flags.
}
\description{
Apply separately approved bounds, allowed values, and missing-value codes to
a data frame or PostgreSQL source without modifying the source. Validation
and aggregate reconciliation complete before the function reports success.
}
\details{
Existing destinations are never replaced. File output is written to
a same-directory staging file, reconciled, and published with a no-replace
operation. PostgreSQL validates and creates one new table with server-side
transformations in a repeatable-read transaction; any validation,
transformation, reconciliation, or commit failure rolls the operation
back. PostgreSQL relations have no physical row-order contract, but the
projection does not filter or explicitly reorder rows.

Numeric values outside approved inclusive bounds become typed missing.
Categorical and binary values outside a populated allowed set become typed
missing. Approved missing codes become typed missing. Existing missing
values remain missing. Data-frame row count, row order, row names, column
order and vector storage are preserved. The source object or relation is
never changed.

The returned audit and custom display methods omit source names, paths,
relation identities, rule values, approval references, and row-level data.
Aggregate counts and hashes can still be sensitive in context and require
caller review before sharing.
}
