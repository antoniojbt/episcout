% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_stratified.R
\name{epi_eda_profile_stratified}
\alias{epi_eda_profile_stratified}
\title{Profile specification-aware stratified summaries}
\usage{
epi_eda_profile_stratified(
  data,
  spec,
  strata,
  include_overall = TRUE,
  include_missing_stratum = TRUE
)
}
\arguments{
\item{data}{A data frame containing observed or prepared data, or an
\code{\link[=epi_eda_postgres_source]{epi_eda_postgres_source()}}.}

\item{spec}{An EDA specification accepted by \code{\link[=epi_eda_spec]{epi_eda_spec()}}.}

\item{strata}{A single character name of a categorical or binary variable.}

\item{include_overall}{Include an Overall group calculated directly from the
included analysis population.}

\item{include_missing_stratum}{Retain standard and declared-sentinel missing
strata as an explicit group. When false, metadata records omitted rows and
Overall describes only retained rows.}
}
\value{
An \code{epi_eda_stratified} list with \code{groups}, \code{variables}, \code{numeric},
\code{categorical}, \code{text}, \code{temporal}, \code{skipped}, and \code{metadata} data frames.
Text output contains aggregate diagnostics only. No files are written.
}
\description{
Calculate canonical descriptive summaries overall and across exactly one
categorical or binary specification variable. Results are long-form,
machine-readable, and retain declared empty, unexpected, and missing strata.
}
\details{
PostgreSQL sources are profiled inside one read-only repeatable-read
transaction. Queries return grouped or scalar aggregates only. Shapiro-Wilk
p-values are \code{NA} for PostgreSQL stratified numeric summaries because that
calculation would require collecting an analysis-value vector; all other
supported canonical numeric fields remain aggregate queries.
}
