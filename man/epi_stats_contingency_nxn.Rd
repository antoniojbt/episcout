% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_stats_contigency_nxn.R
\name{epi_stats_contingency_nxn}
\alias{epi_stats_contingency_nxn}
\title{Generate a Wide-Format Summary Table for Multiple Variables}
\usage{
epi_stats_contingency_nxn(df, dep_var, ind_vars)
}
\arguments{
\item{df}{A data frame containing the data to analyze.}

\item{dep_var}{A string specifying the dependent variable (column in \code{df}).}

\item{ind_vars}{A character vector specifying the independent variable(s) (columns in \code{df}).}
}
\value{
A data frame summarizing the relationships between the dependent variable and the independent variable(s).
}
\description{
This function creates a summary table for a dependent variable and one or more independent variables.
It calculates frequencies, reshapes the data into a wide format, and adds totals and percentages dynamically.
}
\examples{
# Example dataset
set.seed(42)
test_data <- data.frame(
  Outcome = sample(c("Yes", "No"), 100, replace = TRUE),
  Group = sample(c("A", "B", "C"), 100, replace = TRUE),
  Gender = sample(c("Male", "Female"), 100, replace = TRUE)
)

# Generate a summary table
result <- epi_stats_contingency_nxn(test_data, dep_var = "Outcome", ind_vars = c("Group", "Gender"))
print(result)

}
