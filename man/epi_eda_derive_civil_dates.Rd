% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_civil_dates.R
\name{epi_eda_derive_civil_dates}
\alias{epi_eda_derive_civil_dates}
\title{Derive reviewed civil dates from local timestamps}
\usage{
epi_eda_derive_civil_dates(
  data,
  operations,
  variable_keys,
  output_path = NULL,
  output_format = NULL,
  destination_schema = NULL,
  destination_table = NULL
)
}
\arguments{
\item{data}{A data frame or an \code{\link[=epi_eda_postgres_source]{epi_eda_postgres_source()}}.}

\item{operations}{An unmodified object returned by
\code{\link[=epi_eda_approved_civil_dates]{epi_eda_approved_civil_dates()}}.}

\item{variable_keys}{A caller-owned data frame containing exactly \code{name} and
\code{variable_key}. Every approved source key must resolve exactly once.
Additional mappings are permitted.}

\item{output_path}{For a data-frame source, an optional new destination file.}

\item{output_format}{When \code{output_path} is supplied, exactly \code{"csv"} or
\code{"rds"}. The format is never inferred.}

\item{destination_schema}{For a PostgreSQL source, the existing schema in
which to create the new destination table.}

\item{destination_table}{For a PostgreSQL source, the new table name.}
}
\value{
An \code{epi_eda_civil_date_result} list with \code{data} and \code{audit}. For a
data-frame source, \code{data} is the complete derived data frame, including
when it was also exported. For PostgreSQL, \code{data} is \code{NULL} because rows
remain server-side. \code{audit} contains one aggregate \code{summary} row and an
opaque-keyed \code{operations} table with source and derived missing counts,
dimensions, a deterministic operation hash and reconciliation flags.
}
\description{
Apply separately approved civil-date operations to strict local timestamps
in a data frame or PostgreSQL source without modifying or replacing the
source. Every non-missing source value must be exact midnight before any
derived column or destination is created.
}
\details{
Civil-date meaning must be declared in the approved operation;
storage and observed values never establish that meaning. In memory,
supported local timestamps are exact base character vectors using
\verb{YYYY-MM-DD HH:MM:SS} with an optional decimal fraction. Years 0001 through
9999 and valid proleptic Gregorian calendar dates are supported. Offset,
zone and \code{T} syntax and \code{POSIXct}/\code{POSIXlt} vectors are rejected rather than
interpreted. PostgreSQL support is limited to \verb{timestamp without time zone}; the existing rejection of that storage for instant-oriented
preparation is unchanged.

Exact midnight requires zero hour, minute and second and only zero digits
in an optional fraction. If any non-missing value fails, the complete call
is blocked and the error reports only the aggregate affected-value count.
PostgreSQL non-finite timestamps count as affected values. Missing sources
produce missing \code{Date} values. Successful derivation appends separate
columns and leaves every source value unchanged.

Existing file destinations are never replaced. PostgreSQL validation,
aggregate midnight counting, creation and reconciliation occur in one
repeatable-read transaction; failure rolls back the new table. Neither
backend assigns a timezone, converts a timezone or treats a local timestamp
as an instant. Returned audits and display methods omit source and derived
names, paths, relation identities, timestamps and approval references.
Aggregate counts and hashes can still be sensitive in context.
}
