% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_geo.R
\name{epi_geo_write}
\alias{epi_geo_write}
\title{Write a reviewed layer to GeoPackage safely}
\usage{
epi_geo_write(x, dsn, layer, overwrite = FALSE)
}
\arguments{
\item{x}{A reviewed \code{sf} object supported by the phase-A spatial contract.}

\item{dsn}{One local \code{.gpkg} destination path whose parent directory exists.}

\item{layer}{One non-empty layer name.}

\item{overwrite}{Whether an existing layer with the exact name may be
replaced. Unrelated layers are retained.}
}
\value{
\code{dsn}, invisibly, after successful publication.
}
\description{
Stage, re-read and reconcile one named layer before publishing a local
GeoPackage. Existing layers are never replaced silently, and replacement
authority applies only to the named layer.
}
\details{
Publication uses a same-directory staging file and verifies feature
count, attribute names and supported types, geometry types, CRS and bounds
after re-reading. Factor attributes are written and reconciled as text.
Failure before publication leaves the previous destination unchanged;
failure during replacement restores the previous complete file when the
file system permits it. Shapefile export and append semantics are not
supported.
}
