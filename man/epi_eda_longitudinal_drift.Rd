% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_longitudinal_drift.R
\name{epi_eda_longitudinal_drift}
\alias{epi_eda_longitudinal_drift}
\title{Compare reviewed variables across ordered PostgreSQL periods}
\usage{
epi_eda_longitudinal_drift(sources, spec, variables = NULL, max_levels = 50L)
}
\arguments{
\item{sources}{A uniquely named list of at least two unmodified
\code{\link[=epi_eda_postgres_source]{epi_eda_postgres_source()}} objects sharing one caller-owned connection.
List order defines period order.}

\item{spec}{A validated EDA specification data frame as returned by
\code{\link[=epi_eda_spec]{epi_eda_spec()}}.}

\item{variables}{\code{NULL} to use every specification row in specification
order, or a unique non-empty character vector of specification variable
names. Explicit order is retained.}

\item{max_levels}{Positive whole-number hard bound for each declared
categorical domain, each observed period domain, and each adjacent union.}
}
\value{
An \code{epi_eda_longitudinal_drift} list with fixed components
\code{metadata}, \code{schema}, \code{missingness}, \code{missingness_adjacent}, \code{numeric},
\code{numeric_adjacent}, \code{categorical}, \code{categorical_adjacent}, \code{temporal},
\code{temporal_adjacent}, and \code{skipped}.
}
\description{
Produce descriptive schema, missingness, and distribution-drift evidence
for reviewed EDA variables across explicitly ordered completed periods.
Source rows and row identifiers remain inside PostgreSQL.
}
\details{
All source validation and calculations occur in one \verb{REPEATABLE READ READ ONLY} transaction. Counts use the canonical R integer contract and
fail before exceeding \code{.Machine$integer.max}. Categorical retrieval is
limited to \code{max_levels + 1}; any declared, period, or adjacent domain above
the bound fails the whole call. No source rows, identifiers, hypothesis
tests, Shapiro-Wilk values, outlier rules, anomaly scores, or inferred
interpretations are returned or calculated.

Missingness uses standard PostgreSQL missing values plus reviewed
semicolon-delimited \code{missing_codes}. Numeric location and spread retain the
canonical finite-value, type-7 quantile, and sample-SD rules. Categorical
proportions name both total-row and observed-value denominators. Dates use
days and timestamps with time zone use UTC seconds. Text participates in
schema and missingness continuity but has no distribution-drift summary.

Adjacent changes are signed right-minus-left changes on the original scale;
they are not magnitudes or thresholds. Missingness relative change is
available only when its explicit left-period proportion denominator is
positive. Unavailable distribution evidence is recorded with stable
\code{code} values: \code{absent_variable}, \code{incompatible_type},
\code{invalid_missing_contract}, \code{unsupported_analysis_type}, or
\code{zero_denominator}.

Any invalid input, modified source, connection, catalogue, query,
transaction, count-range, categorical-bound, or reconciliation failure is a
hard error and returns no partial object. The operation creates no database
object and leaves the caller-owned connection open and idle.
}
\section{Result schema}{

\code{metadata} records the contract version; period, full-specification and selected-variable counts; ordered period labels, source fingerprints and resolved variables as list columns; source-set, full-specification and selected-specification SHA-256 fingerprints; \code{max_levels}; the count contract and maximum; and the snapshot mode.

Every period table starts with integer \code{period_index}, \code{period}, integer
\code{variable_index}, and \code{variable}. \code{schema} then has \code{analysis_type},
\code{expected_database_type}, \code{observed_type}, \code{observed_present},
\code{type_status}, and \code{type_reason}. \code{missingness} has integer \code{n},
\code{n_missing}, and \code{n_observed}, \code{p_missing}, availability \code{status}, and
machine-readable \code{reason}.

\code{missingness_adjacent} names both periods and retains their left and right
counts and missing proportions, signed \code{absolute_change}, \code{relative_change},
and its explicit \code{relative_denominator}. \code{numeric} has integer \code{n},
\code{n_missing}, \code{n_observed}, \code{n_infinite}, and \code{n_finite}; \code{min}, \code{q1},
\code{mean}, \code{median}, \code{q3}, \code{max}, \code{iqr}, and \code{sd}; and availability fields.
\code{numeric_adjacent} retains left and right \code{n_finite} and the left value,
right value, and signed \verb{*_change} for each authorised numeric metric.

\code{categorical} has \code{level}, integer \code{n}, \code{p_total}, \code{p_observed},
\code{is_declared}, \code{is_unexpected}, and availability fields.
\code{categorical_adjacent} names both denominator pairs, left and right counts
and proportions, signed proportion differences, declaration flags, the
positive-count \code{level_status}, and availability fields. \code{temporal} has
integer \code{n}, \code{n_missing}, and \code{n_observed}; formatted \code{min} and \code{max};
numeric \code{range_value}; \code{unit}; and availability fields.
\code{temporal_adjacent} retains both formatted endpoints, numeric endpoint
shifts, both range values, signed range change, the unit, and availability
fields.

\code{skipped} starts with the period/variable keys and adds \code{component}, \code{code},
and a value-free explanatory \code{message}. Typed zero-row tables retain these
schemas.
}

\seealso{
Other EDA:
\code{\link[=epi_eda_identifier_qc]{epi_eda_identifier_qc()}},
\code{\link[=epi_eda_identifier_qc_spec]{epi_eda_identifier_qc_spec()}},
\code{\link[=epi_eda_longitudinal_qc]{epi_eda_longitudinal_qc()}},
\code{\link[=epi_eda_longitudinal_transitions]{epi_eda_longitudinal_transitions()}},
\code{\link[=epi_eda_render_identifier_qc_report]{epi_eda_render_identifier_qc_report()}}
}
\concept{EDA}
