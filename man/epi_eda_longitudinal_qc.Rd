% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_longitudinal_qc.R
\name{epi_eda_longitudinal_qc}
\alias{epi_eda_longitudinal_qc}
\title{Audit longitudinal population membership and record keys in PostgreSQL}
\usage{
epi_eda_longitudinal_qc(sources, entity_id, record_key = NULL)
}
\arguments{
\item{sources}{A uniquely named list of at least two unmodified
\code{\link[=epi_eda_postgres_source]{epi_eda_postgres_source()}} objects that share one caller-owned
connection. List order defines period order.}

\item{entity_id}{One column name present in every source. Supported common
PostgreSQL families are text/varchar, integral, and UUID.}

\item{record_key}{\code{NULL}, or one or more unique column names that together
define within-period record uniqueness. Only complete keys are included
in distinct and duplicate calculations.}
}
\value{
An \code{epi_eda_longitudinal_qc} list with value-free \code{metadata},
\code{period_summary}, \code{adjacent_membership}, \code{pairwise_overlap},
\code{history_summary}, and typed \code{issues} components.
}
\description{
Compare aggregate entity membership across explicitly ordered completed
periods and, when declared, inspect within-period record-key uniqueness.
Entity identifiers and record-key values remain inside PostgreSQL.
}
\details{
All sources are validated before and inside one \verb{REPEATABLE READ READ ONLY} transaction. Null entity identifiers and blank text
representations are excluded from membership. Counts are returned as
doubles only after exact decimal results are checked not to exceed
\code{2^53 - 1}; proportions are \code{NA_real_} when their named denominator is
zero. Population changes, reappearance, and repeated entity rows are
descriptive and do not create findings. The only findings are warnings for
an empty period, invalid entity identifiers, missing record keys, or
duplicate complete record keys.

The operation is aggregate-only, creates no database objects, and leaves
the connection open and idle. Any validation, snapshot, query, count-range,
or reconciliation failure is a hard error and returns no partial result.

Entity columns must share one text/varchar, integral, or UUID family; text
collations must be deterministic. Every declared record-key column must use
the same base type in every source. Supported key base types are boolean,
integral, floating-point and numeric types, text/varchar/bpchar, date,
timestamp/timestamptz, time/timetz, and UUID; collatable key columns must use
deterministic equality.
}
\section{Result schema}{

\code{metadata} has \code{contract_version}, integer \code{n_periods}, \code{entity_id},
\code{record_key_declared}, integer \code{n_record_key_columns}, machine-readable
ordered \code{period_labels} and \code{source_fingerprints} list columns, and
\code{source_set_fingerprint_sha256}.

\code{period_summary} has integer \code{period_index}, \code{period_label}, and double
\code{n_rows}, \code{n_entity_null}, \code{n_entity_blank}, \code{n_entity_nonblank},
\code{n_valid_entity_rows},
\code{n_distinct_entities}, \code{n_repeated_entity_rows},
\code{n_repeated_entity_excess}, \code{max_entity_frequency},
\code{n_missing_record_key}, \code{n_complete_record_key_rows},
\code{n_distinct_record_keys}, \code{n_duplicate_record_key_groups},
\code{n_duplicate_record_key_rows}, \code{n_duplicate_record_key_excess}, and
\code{max_record_key_frequency}. Record-key fields are \code{NA_real_} when no key is
declared.

\code{adjacent_membership} has integer \code{from_period_index} and \code{to_period_index},
their labels, double entity counts \code{n_from_entities}, \code{n_to_entities},
\code{n_union}, \code{n_retained}, \code{n_exited}, and \code{n_entered}, explicit
\code{retention_numerator} and \code{entry_numerator}, and explicit double retention,
exit, and entry denominator/proportion pairs.

\code{pairwise_overlap} has integer \code{left_period_index} and
\code{right_period_index}, their labels, double \code{n_left_entities},
\code{n_right_entities}, \code{n_overlap}, \code{n_union}, \code{n_left_only}, and \code{n_right_only}, and
explicit double left/right overlap denominator/proportion pairs.

\code{history_summary} has integer first/last positions and their labels, integer
\code{periods_observed} and \code{gap_periods}, logical \code{has_gap}, and double
\code{n_entities}, \code{proportion_denominator}, and \code{proportion}.

\code{issues} always has character \code{issue_code} and \code{severity}, integer
\code{period_index}, character \code{period} and \code{variable}, double \code{n_affected},
and character \code{message}, including in its typed zero-row form.
}

\seealso{
Other EDA:
\code{\link[=epi_eda_identifier_qc]{epi_eda_identifier_qc()}},
\code{\link[=epi_eda_identifier_qc_spec]{epi_eda_identifier_qc_spec()}},
\code{\link[=epi_eda_longitudinal_drift]{epi_eda_longitudinal_drift()}},
\code{\link[=epi_eda_longitudinal_transitions]{epi_eda_longitudinal_transitions()}},
\code{\link[=epi_eda_render_identifier_qc_report]{epi_eda_render_identifier_qc_report()}}
}
\concept{EDA}
