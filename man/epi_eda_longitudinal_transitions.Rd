% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_longitudinal_transitions.R
\name{epi_eda_longitudinal_transitions}
\alias{epi_eda_longitudinal_transitions}
\title{Summarise categorical state transitions across PostgreSQL periods}
\usage{
epi_eda_longitudinal_transitions(
  sources,
  entity_id,
  spec,
  variables,
  max_levels = 50L
)
}
\arguments{
\item{sources}{A uniquely named list of at least two unmodified
\code{\link[=epi_eda_postgres_source]{epi_eda_postgres_source()}} objects sharing one caller-owned connection.
List order defines period order.}

\item{entity_id}{One column name present in every source. Supported common
PostgreSQL families are text/varchar, integral, and UUID.}

\item{spec}{A validated EDA specification data frame as returned by
\code{\link[=epi_eda_spec]{epi_eda_spec()}}.}

\item{variables}{A unique, non-empty character vector of specification
variable names. Caller order is retained. Entity identifiers and variables
whose specification role is \code{id} or \code{identifier} are prohibited.}

\item{max_levels}{Positive whole-number hard bound for each declared domain,
each usable period domain, and each adjacent union. V1 accepts at most 50
states, so a complete transition matrix has at most 2,500 cells.}
}
\value{
An \code{epi_eda_longitudinal_transitions} list with fixed components
\code{metadata}, \code{state_audit}, \code{transition_summary}, \code{transition_counts}, and
\code{issues}.
}
\description{
Summarise the same caller-selected categorical or binary variables for
distinct valid entities retained across adjacent completed periods. Entity
reconciliation and transition aggregation remain inside PostgreSQL; only
bounded state labels and aggregate counts are returned.
}
\details{
Null entity identifiers and blank text representations are excluded
from period membership. Repeated entity rows do not multiply membership.
Within each entity-period, zero distinct non-missing canonical values is a
missing state, exactly one is usable, and more than one is conflicting.
Repeated identical values and one value mixed with missing rows remain
usable. Conflict takes precedence over missingness when adjacent retained
entities are excluded.

Only adjacent periods are compared. \code{n_retained} describes the distinct
valid-entity intersection. \code{n_eligible} includes retained entities with one
usable state on both sides; it is also the transition-proportion denominator.
Missing and conflicting exclusions reconcile with the retained population.
A zero eligible denominator produces \code{NA} proportions and a \code{zero_eligible}
warning rather than treating proportions as zero.

Each available pair returns the complete square domain: first-occurrence
deduplicated declared levels in specification order, followed by unexpected
usable states observed on either side in bytewise order. Zero-count cells are
retained. Missing values are never a state. Bound excess, count overflow,
query failure, or reconciliation failure aborts the whole call without a
partial result.

All source validation and calculations occur in one \verb{REPEATABLE READ READ ONLY} transaction. The operation creates no database objects, collects no
row-level records or entity values, and leaves the caller-owned connection
open and idle. The result is generic descriptive evidence: it does not infer
scientific desirability, plausibility, entry or exit meaning, cross-variable
transitions, or project-specific derived concepts.

\code{epi_eda_longitudinal_qc()} separately describes population continuity,
entry, and exit. \code{epi_eda_longitudinal_drift()} separately describes
period-level schema, missingness, and marginal distributions without joining
retained entities.
}
\section{Result schema}{

\code{metadata} is one row recording contract version, period and variable counts,
ordered labels, source and specification fingerprints, selected variables,
\code{entity_id}, \code{max_levels}, the exact-base-R-double count contract and maximum,
and snapshot mode.

\code{state_audit} has integer \code{period_index}, \code{period}, integer \code{variable_index},
\code{variable}, double \code{n_valid_entities}, \code{n_usable_state}, \code{n_missing_state}, and
\code{n_conflicting_state}, plus \code{status} and machine-readable \code{reason}.

\code{transition_summary} names the left and right periods and variable, then has
double \code{n_retained}, \code{n_eligible}, \code{n_excluded_missing}, and
\code{n_excluded_conflict}, integer \code{n_transition_cells}, double
\code{eligible_denominator}, and availability fields. \code{transition_counts} adds
\code{from_state}, \code{to_state}, double \code{n}, \code{eligible_denominator}, \code{proportion},
declaration flags for each side, and availability fields.

\code{issues} has \code{issue_code}, \code{severity}, period and adjacent-pair scope fields,
integer variable keys, double \code{n_affected}, and a value-free \code{message}.
Period unavailability issues precede period conflicts, which precede pair
zero-denominator warnings.
}

\seealso{
Other EDA:
\code{\link[=epi_eda_identifier_qc]{epi_eda_identifier_qc()}},
\code{\link[=epi_eda_identifier_qc_spec]{epi_eda_identifier_qc_spec()}},
\code{\link[=epi_eda_longitudinal_drift]{epi_eda_longitudinal_drift()}},
\code{\link[=epi_eda_longitudinal_qc]{epi_eda_longitudinal_qc()}},
\code{\link[=epi_eda_render_identifier_qc_report]{epi_eda_render_identifier_qc_report()}}
}
\concept{EDA}
