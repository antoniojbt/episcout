% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_qc_proposals.R
\name{epi_eda_qc_proposals}
\alias{epi_eda_qc_proposals}
\title{Generate reviewable QC evidence and pending cleaning-rule proposals}
\usage{
epi_eda_qc_proposals(data, spec, variable_keys)
}
\arguments{
\item{data}{A data frame or an \code{\link[=epi_eda_postgres_source]{epi_eda_postgres_source()}} containing the variables declared by \code{spec}.}

\item{spec}{An EDA specification data frame or CSV path accepted by \code{\link[=epi_eda_spec]{epi_eda_spec()}}.}

\item{variable_keys}{A data frame containing exactly \code{name} and \code{variable_key}. Names must cover the normalised specification exactly once. Keys are caller-created, persisted opaque identifiers matching \verb{^var_[a-z0-9]\{16,64\}$} and must be unique.}
}
\value{
An \code{epi_eda_qc_proposals} list containing exactly \code{evidence} and \code{proposals}. \code{evidence} has one row per specification row and the columns \code{variable_key}, \code{evidence_state}, \code{declared_type}, \code{profile_status}, \code{evidence_code}, \code{n}, \code{n_missing}, \code{n_observed}, \code{n_unique}, \code{n_infinite}, \code{n_finite}, \code{observed_min}, \code{observed_max}, \code{tukey_lower_fence}, \code{tukey_upper_fence}, \code{n_below_tukey}, and \code{n_above_tukey}. \code{proposals} has zero or one pending row per variable and the columns \code{variable_key}, \code{proposal_state}, \code{candidate_type}, \code{units_review_required}, \code{candidate_units}, \code{candidate_screening_min}, \code{candidate_screening_max}, \code{screening_basis}, \code{candidate_allowed_levels}, \code{candidate_missing_codes}, and \code{rationale_codes}.
}
\description{
Profile aggregate quality-control evidence without changing observations or the reviewed EDA specification. Deterministic proposal fields are prompts for analyst review only; they are neither approved nor executable rules.
}
\details{
Source names are used only to look up caller-managed keys and are not returned. Observed extrema and Tukey 1.5-IQR fences are descriptive signals from the profiled snapshot, not scientific validity limits. An integral numeric or integer variable with exactly both zero and one observed may receive a pending binary candidate, but its reviewed type and values are never changed. Units and missing-value codes are never inferred.

The reviewed specification remains authoritative for semantic \code{type}, \code{units}, \code{levels}, \code{min}, \code{max}, and \code{missing_codes}. This function does not approve or apply rules, clean data, return a modified specification, or write files or database objects. Aggregate results can still be sensitive in context and require caller review before saving or sharing.
}
