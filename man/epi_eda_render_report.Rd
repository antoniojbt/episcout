% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_report.R
\name{epi_eda_render_report}
\alias{epi_eda_render_report}
\title{Render a specification-first EDA report}
\usage{
epi_eda_render_report(
  data,
  spec,
  output_dir,
  synthetic = FALSE,
  n = 100,
  seed = NULL,
  quiet = TRUE,
  maps = FALSE,
  map_vars = character(),
  max_map_points = 10000L,
  data_origin = NULL
)
}
\arguments{
\item{data}{A data frame containing observed or caller-declared synthetic data. Required when \code{synthetic = FALSE}; ignored when \code{synthetic = TRUE}.}

\item{spec}{An EDA specification data frame or CSV path accepted by \code{\link[=epi_eda_spec]{epi_eda_spec()}}.}

\item{output_dir}{Directory where machine-readable workflow outputs and the rendered report are written. The directory must already exist.}

\item{synthetic}{Logical; when \code{TRUE}, generate synthetic data from \code{spec} before running the workflow.}

\item{n}{Number of synthetic rows to generate when \code{synthetic = TRUE}.}

\item{seed}{Optional random seed passed to \code{\link[=epi_eda_generate_synthetic_data]{epi_eda_generate_synthetic_data()}}.}

\item{quiet}{Logical; passed to \code{\link[rmarkdown:render]{rmarkdown::render()}} to control render output.}

\item{maps}{Whether to create one geometry-only point map for every declared
coordinate pair.}

\item{map_vars}{Unique declared variables for additional thematic maps.}

\item{max_map_points}{Inclusive maximum number of rows allowed for mapping.}

\item{data_origin}{Optional canonical origin for \code{data}. One of \code{"observed"},
\code{"episcout_generated_synthetic"} or \code{"caller_declared_synthetic"}.
When omitted, \code{synthetic = FALSE} maps to \code{"observed"} and
\code{synthetic = TRUE} maps to \code{"episcout_generated_synthetic"}.}
}
\value{
A single character string containing the rendered HTML report path.
}
\description{
Run the specification-first EDA workflow and render a bundled Quarto HTML report from the workflow outputs.
}
