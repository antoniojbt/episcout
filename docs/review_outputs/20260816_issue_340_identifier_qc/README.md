# Identifier-QC synthetic review fixture

This tracked bundle demonstrates the generic aggregate-only report layout for
EpiScout issue #340. It contains no source identifiers, source rows, database
connection information or project-specific policy.

Open `index.html` for the reviewer entry point. `identifier_qc.csv` and
`availability_qc.csv` are synthetic aggregate tables; `manifest.csv` records
the fixture contract.
